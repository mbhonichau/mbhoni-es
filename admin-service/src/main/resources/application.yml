spring:
  config:
    import: optional:configserver:http://localhost:8888

  application:
    name: admin-service

  # OPTIMIZATION: Disable Thymeleaf caching for "Instant" UI updates
  thymeleaf:
    cache: false

  # OPTIMIZATION: Fine-tune DevTools restart behavior
  devtools:
    restart:
      enabled: true
      exclude: static/**,public/**,templates/** # Don't restart JVM for CSS/HTML changes
    livereload:
      enabled: true

  datasource:
    url: jdbc:mysql://localhost:3306/mbhoni_es?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Password@12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: creativembhoni@gmail.com
    password: aasx npth cgst ykun
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    from: noreply@mbhoni-es.com

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
    health-check-url-path: /actuator/health
    status-page-url-path: /actuator/info

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
    initial-instance-info-replication-interval-seconds: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

app:
  admin-email: creativembhoni@gmail.com

server:
  port: 8080
